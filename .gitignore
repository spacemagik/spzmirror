# macOS system files
.DS_Store
.AppleDouble
.LSOverride
._*

# Cursor workspace config (kept private, not shared on GitHub)
.cursor/

# Node / Vite
node_modules/
dist/
.vite/
*.log

# Block .spz files everywhere except the default splat in public/
*.spz
!public/*.spz
